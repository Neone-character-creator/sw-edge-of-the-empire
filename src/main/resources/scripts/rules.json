{"advancement":{"characteristic":{"description":"Increase Characteristic","uses":"$state.character.availableExperience","options":["brawn","agility","intellect","cunning","willpower","presence"],"when":"# $state.character.characteristics[$this] < 6 ","cost":"# ($state.character.characteristics[$this] + 1) * 10","effect":"$state.character.characteristics[$this] = $state.character.characteristics[$this] += 1"},"skill":{"description":"Skill Training","uses":"$state.character.availableExperience","options":"# Object.keys($model.skills.prototype.definition) ","when":"# $state.character.skills[$this].rank < 5 ","cost":"# ($state.character.skills[$this].rank + 1) * 5 ","effect":"# $state.character.skills[$this].rank += 1"},"talent":{"description":"Acquire Talent","uses":"$state.character.availableExperience","options":"# $state.character.specializations.reduce((combined, s)=> combined.concat(s.talents), []) ","when":"# $this.tier === 1 || $state.character.specializations.reduce((combined, x)=> combined.concat(x.talents), []).find(y => $state.character.specializationTalents.find(z => z.id === y.id))","cost":"# $this.tier * 5"}}}